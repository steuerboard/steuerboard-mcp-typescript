/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminClientsGetClient } from "../../funcs/adminClientsGetClient.js";
import { GetClientRequest$zodSchema } from "../../models/getclientop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetClientRequest$zodSchema,
};

export const tool$adminClientsGetClient: ToolDefinition<typeof args> = {
  name: "admin-clients-get-client",
  description: `Get a client

Returns a single client by ID.`,
  annotations: {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminClientsGetClient(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
