/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminUsersDeleteAccountantUser } from "../../funcs/adminUsersDeleteAccountantUser.js";
import { DeleteAccountantUserRequest$zodSchema } from "../../models/deleteaccountantuserop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteAccountantUserRequest$zodSchema,
};

export const tool$adminUsersDeleteAccountantUser: ToolDefinition<typeof args> =
  {
    name: "admin-users-delete-accountant-user",
    description: `Delete an accountant user

Deletes an accountant user by ID.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await adminUsersDeleteAccountantUser(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
