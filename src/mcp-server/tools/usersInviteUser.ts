/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersInviteUser } from "../../funcs/usersInviteUser.js";
import { InviteUserRequest$zodSchema } from "../../models/inviteuserop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: InviteUserRequest$zodSchema,
};

export const tool$usersInviteUser: ToolDefinition<typeof args> = {
  name: "users-invite-user",
  description: `Invite a user

Invites a new client user and returns the created record.`,
  annotations: {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersInviteUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
