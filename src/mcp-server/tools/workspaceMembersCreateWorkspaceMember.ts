/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workspaceMembersCreateWorkspaceMember } from "../../funcs/workspaceMembersCreateWorkspaceMember.js";
import { CreateWorkspaceMemberRequest$zodSchema } from "../../models/createworkspacememberop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateWorkspaceMemberRequest$zodSchema,
};

export const tool$workspaceMembersCreateWorkspaceMember: ToolDefinition<
  typeof args
> = {
  name: "workspace-members-create-workspace-member",
  description: `Create a workspace member

Creates a new workspace member.`,
  annotations: {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workspaceMembersCreateWorkspaceMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
