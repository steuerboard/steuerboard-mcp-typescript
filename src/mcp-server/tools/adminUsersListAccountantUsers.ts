/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminUsersListAccountantUsers } from "../../funcs/adminUsersListAccountantUsers.js";
import { ListAccountantUsersRequest$zodSchema } from "../../models/listaccountantusersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListAccountantUsersRequest$zodSchema.optional(),
};

export const tool$adminUsersListAccountantUsers: ToolDefinition<typeof args> = {
  name: "admin-users-list-accountant-users",
  description: `List accountant users

Returns a paginated list of accountant users.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminUsersListAccountantUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
