/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectSessionsRequestLinkCollectToken } from "../../funcs/collectSessionsRequestLinkCollectToken.js";
import { RequestLinkCollectTokenRequest$zodSchema } from "../../models/requestlinkcollecttokenop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RequestLinkCollectTokenRequest$zodSchema,
};

export const tool$collectSessionsRequestLinkCollectToken: ToolDefinition<
  typeof args
> = {
  name: "collect-sessions-request-link-collect-token",
  description: `Request a new collect session link

Sends the current active link when possible, or creates a new session for an expired token. The recipient comes from the session itself, so no body is needed. Always returns 202 to prevent enumeration.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectSessionsRequestLinkCollectToken(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
