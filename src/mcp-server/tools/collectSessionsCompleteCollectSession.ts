/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectSessionsCompleteCollectSession } from "../../funcs/collectSessionsCompleteCollectSession.js";
import { CompleteCollectSessionRequest$zodSchema } from "../../models/completecollectsessionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CompleteCollectSessionRequest$zodSchema,
};

export const tool$collectSessionsCompleteCollectSession: ToolDefinition<
  typeof args
> = {
  name: "collect-sessions-complete-collect-session",
  description: `Complete collect session

Marks the session as completed by the client, optionally with a note about missing documents.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectSessionsCompleteCollectSession(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
