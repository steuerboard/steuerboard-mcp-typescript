/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workspaceMembersRemoveWorkspaceMember } from "../../funcs/workspaceMembersRemoveWorkspaceMember.js";
import { RemoveWorkspaceMemberRequest$zodSchema } from "../../models/removeworkspacememberop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RemoveWorkspaceMemberRequest$zodSchema,
};

export const tool$workspaceMembersRemoveWorkspaceMember: ToolDefinition<
  typeof args
> = {
  name: "workspace-members-remove-workspace-member",
  description: `Remove a workspace member

Removes a workspace member from a workspace.`,
  annotations: {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workspaceMembersRemoveWorkspaceMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
