/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { filesDownloadFileContent } from "../../funcs/filesDownloadFileContent.js";
import { DownloadFileContentRequest$zodSchema } from "../../models/downloadfilecontentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DownloadFileContentRequest$zodSchema,
};

export const tool$filesDownloadFileContent: ToolDefinition<typeof args> = {
  name: "files-download-file-content",
  description: `Download file content

Returns a temporary presigned URL to download the file. The URL expires after 10 minutes.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await filesDownloadFileContent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
