/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersUpdateUser } from "../../funcs/usersUpdateUser.js";
import { UpdateUserRequest$zodSchema } from "../../models/updateuserop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateUserRequest$zodSchema,
};

export const tool$usersUpdateUser: ToolDefinition<typeof args> = {
  name: "users-update-user",
  description: `Update a user

Updates a client user by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersUpdateUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
