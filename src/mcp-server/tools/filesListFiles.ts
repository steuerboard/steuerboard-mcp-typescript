/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { filesListFiles } from "../../funcs/filesListFiles.js";
import { ListFilesRequest$zodSchema } from "../../models/listfilesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListFilesRequest$zodSchema,
};

export const tool$filesListFiles: ToolDefinition<typeof args> = {
  name: "files-list-files",
  description: `List files

Returns a paginated list of files with optional workspace filters.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await filesListFiles(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
