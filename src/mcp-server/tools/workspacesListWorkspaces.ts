/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workspacesListWorkspaces } from "../../funcs/workspacesListWorkspaces.js";
import { ListWorkspacesRequest$zodSchema } from "../../models/listworkspacesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListWorkspacesRequest$zodSchema,
};

export const tool$workspacesListWorkspaces: ToolDefinition<typeof args> = {
  name: "workspaces-list-workspaces",
  description: `List workspaces

Returns a paginated list of workspaces.`,
  annotations: {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workspacesListWorkspaces(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
