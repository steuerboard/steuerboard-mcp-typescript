/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workspacesUpdateWorkspace } from "../../funcs/workspacesUpdateWorkspace.js";
import { UpdateWorkspaceRequest$zodSchema } from "../../models/updateworkspaceop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateWorkspaceRequest$zodSchema,
};

export const tool$workspacesUpdateWorkspace: ToolDefinition<typeof args> = {
  name: "workspaces-update-workspace",
  description: `Update a workspace

Updates a workspace by ID.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workspacesUpdateWorkspace(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
