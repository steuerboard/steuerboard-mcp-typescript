/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminUsersGetAccountantUser } from "../../funcs/adminUsersGetAccountantUser.js";
import { GetAccountantUserRequest$zodSchema } from "../../models/getaccountantuserop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetAccountantUserRequest$zodSchema,
};

export const tool$adminUsersGetAccountantUser: ToolDefinition<typeof args> = {
  name: "admin-users-get-accountant-user",
  description: `Get an accountant user

Returns a single accountant user by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminUsersGetAccountantUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
