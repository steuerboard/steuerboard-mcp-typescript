/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectSessionsPingCollectSession } from "../../funcs/collectSessionsPingCollectSession.js";
import { PingCollectSessionRequest$zodSchema } from "../../models/pingcollectsessionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PingCollectSessionRequest$zodSchema,
};

export const tool$collectSessionsPingCollectSession: ToolDefinition<
  typeof args
> = {
  name: "collect-sessions-ping-collect-session",
  description: `Ping collect session

Fires a session-opened tracking event. Thin, fire-and-forget.`,
  annotations: {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectSessionsPingCollectSession(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
