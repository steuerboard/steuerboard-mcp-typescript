/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectSessionsDeleteCollectSubmission } from "../../funcs/collectSessionsDeleteCollectSubmission.js";
import { DeleteCollectSubmissionRequest$zodSchema } from "../../models/deletecollectsubmissionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteCollectSubmissionRequest$zodSchema,
};

export const tool$collectSessionsDeleteCollectSubmission: ToolDefinition<
  typeof args
> = {
  name: "collect-sessions-delete-collect-submission",
  description: `Delete a collect submission

Deletes a pending submission and its associated file from storage.`,
  annotations: {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectSessionsDeleteCollectSubmission(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
