/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersGetUser } from "../../funcs/usersGetUser.js";
import { GetUserRequest$zodSchema } from "../../models/getuserop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetUserRequest$zodSchema,
};

export const tool$usersGetUser: ToolDefinition<typeof args> = {
  name: "users-get-user",
  description: `Get a user

Returns a single client user by ID.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersGetUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
