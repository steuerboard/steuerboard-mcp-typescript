/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { SteuerboardCore } from "../core.js";
import { appendForm, encodeSimple, normalizeBlob } from "../lib/encodings.js";
import {
  bytesToBlob,
  getContentTypeFromFileName,
  readableStreamToArrayBuffer,
} from "../lib/files.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { pathToFunc } from "../lib/url.js";
import { APIError } from "../models/errors/apierror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import {
  SubmitCollectFileRequest,
  SubmitCollectFileRequest$zodSchema,
  SubmitCollectFileResponse,
  SubmitCollectFileResponse$zodSchema,
} from "../models/submitcollectfileop.js";
import { APICall, APIPromise } from "../types/async.js";
import { isBlobLike } from "../types/blobs.js";
import { Result } from "../types/fp.js";
import { isReadableStream } from "../types/streams.js";

/**
 * Submit a file to a collect session
 *
 * @remarks
 * Uploads a file submission for a collect item via a public token.
 */
export function collectSessionsSubmitCollectFile(
  client$: SteuerboardCore,
  request: SubmitCollectFileRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    SubmitCollectFileResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >
> {
  return new APIPromise($do(
    client$,
    request,
    options,
  ));
}

async function $do(
  client$: SteuerboardCore,
  request: SubmitCollectFileRequest,
  options?: RequestOptions,
): Promise<
  [
    Result<
      SubmitCollectFileResponse,
      | APIError
      | SDKValidationError
      | UnexpectedClientError
      | InvalidRequestError
      | RequestAbortedError
      | RequestTimeoutError
      | ConnectionError
    >,
    APICall,
  ]
> {
  const parsed$ = safeParse(
    request,
    (value$) => SubmitCollectFileRequest$zodSchema.parse(value$),
    "Input validation failed",
  );
  if (!parsed$.ok) {
    return [parsed$, { status: "invalid" }];
  }
  const payload$ = parsed$.value;
  const body$ = new FormData();
  appendForm(
    body$,
    "collectItemId",
    payload$.CollectFileSubmitForm.collectItemId,
  );
  if (isBlobLike(payload$.CollectFileSubmitForm.file)) {
    const file = payload$.CollectFileSubmitForm.file;
    const blob = await normalizeBlob(file);
    const name = "name" in file ? (file.name as string) : undefined;
    appendForm(body$, "file", blob, name);
  } else if (isReadableStream(payload$.CollectFileSubmitForm.file.content)) {
    const buffer = await readableStreamToArrayBuffer(
      payload$.CollectFileSubmitForm.file.content,
    );
    const contentType =
      getContentTypeFromFileName(payload$.CollectFileSubmitForm.file.fileName)
      || "application/octet-stream";
    appendForm(
      body$,
      "file",
      bytesToBlob(buffer, contentType),
      payload$.CollectFileSubmitForm.file.fileName,
    );
  } else {
    const contentType =
      getContentTypeFromFileName(payload$.CollectFileSubmitForm.file.fileName)
      || "application/octet-stream";
    appendForm(
      body$,
      "file",
      bytesToBlob(payload$.CollectFileSubmitForm.file.content, contentType),
      payload$.CollectFileSubmitForm.file.fileName,
    );
  }

  const pathParams$ = {
    token: encodeSimple("token", payload$.token, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path$ = pathToFunc("/collect/sessions/{token}/submit/file")(
    pathParams$,
  );

  const headers$ = new Headers(compactMap({
    Accept: "application/json",
  }));

  const context = {
    options: client$._options,
    baseURL: options?.serverURL ?? client$._baseURL ?? "",
    operationID: "submitCollectFile",
    oAuth2Scopes: null,
    resolvedSecurity: null,
    securitySource: null,
    retryConfig: options?.retries
      || client$._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || [
      "429",
      "500",
      "502",
      "503",
      "504",
    ],
  };

  const requestRes = client$._createRequest(context, {
    method: "POST",
    baseURL: options?.serverURL,
    path: path$,
    headers: headers$,
    body: body$,
    userAgent: client$._options.userAgent,
    timeoutMs: options?.timeoutMs || client$._options.timeoutMs
      || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req$ = requestRes.value;

  const doResult = await client$._do(req$, {
    context,
    errorCodes: [],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req$ }];
  }
  const response = doResult.value;
  const responseFields$ = {
    HttpMeta: { Response: response, Request: req$ },
  };

  const [result$] = await M.match<
    SubmitCollectFileResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >(
    M.json(201, SubmitCollectFileResponse$zodSchema, {
      key: "CollectFileSubmission",
    }),
    M.json(404, SubmitCollectFileResponse$zodSchema, {
      key: "fourHundredAndFourApplicationJsonObject",
    }),
    M.json(410, SubmitCollectFileResponse$zodSchema, { key: "bad_request" }),
    M.json(413, SubmitCollectFileResponse$zodSchema, {
      key: "fourHundredAndThirteenApplicationJsonObject",
    }),
    M.json(422, SubmitCollectFileResponse$zodSchema, {
      key: "fourHundredAndTwentyTwoApplicationJsonOneOf",
    }),
    M.json(429, SubmitCollectFileResponse$zodSchema, { key: "rate_limit" }),
    M.json(500, SubmitCollectFileResponse$zodSchema, { key: "internal_error" }),
  )(response, req$, { extraFields: responseFields$ });

  return [result$, { status: "complete", request: req$, response }];
}
