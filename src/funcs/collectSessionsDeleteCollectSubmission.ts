/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { SteuerboardCore } from "../core.js";
import { encodeSimple } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { pathToFunc } from "../lib/url.js";
import {
  DeleteCollectSubmissionRequest,
  DeleteCollectSubmissionRequest$zodSchema,
  DeleteCollectSubmissionResponseResponse,
  DeleteCollectSubmissionResponseResponse$zodSchema,
} from "../models/deletecollectsubmissionop.js";
import { APIError } from "../models/errors/apierror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * Delete a collect submission
 *
 * @remarks
 * Deletes a pending submission and its associated file from storage.
 */
export function collectSessionsDeleteCollectSubmission(
  client$: SteuerboardCore,
  request: DeleteCollectSubmissionRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    DeleteCollectSubmissionResponseResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >
> {
  return new APIPromise($do(
    client$,
    request,
    options,
  ));
}

async function $do(
  client$: SteuerboardCore,
  request: DeleteCollectSubmissionRequest,
  options?: RequestOptions,
): Promise<
  [
    Result<
      DeleteCollectSubmissionResponseResponse,
      | APIError
      | SDKValidationError
      | UnexpectedClientError
      | InvalidRequestError
      | RequestAbortedError
      | RequestTimeoutError
      | ConnectionError
    >,
    APICall,
  ]
> {
  const parsed$ = safeParse(
    request,
    (value$) => DeleteCollectSubmissionRequest$zodSchema.parse(value$),
    "Input validation failed",
  );
  if (!parsed$.ok) {
    return [parsed$, { status: "invalid" }];
  }
  const payload$ = parsed$.value;
  const body$ = null;

  const pathParams$ = {
    submissionId: encodeSimple("submissionId", payload$.submissionId, {
      explode: false,
      charEncoding: "percent",
    }),
    token: encodeSimple("token", payload$.token, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path$ = pathToFunc("/collect/sessions/{token}/submit/{submissionId}")(
    pathParams$,
  );

  const headers$ = new Headers(compactMap({
    Accept: "application/json",
  }));

  const context = {
    options: client$._options,
    baseURL: options?.serverURL ?? client$._baseURL ?? "",
    operationID: "deleteCollectSubmission",
    oAuth2Scopes: [],
    resolvedSecurity: null,
    securitySource: null,
    retryConfig: options?.retries
      || client$._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || [
      "429",
      "500",
      "502",
      "503",
      "504",
    ],
  };

  const requestRes = client$._createRequest(context, {
    method: "DELETE",
    baseURL: options?.serverURL,
    path: path$,
    headers: headers$,
    body: body$,
    userAgent: client$._options.userAgent,
    timeoutMs: options?.timeoutMs || client$._options.timeoutMs
      || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req$ = requestRes.value;

  const doResult = await client$._do(req$, {
    context,
    errorCodes: [],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req$ }];
  }
  const response = doResult.value;
  const responseFields$ = {
    HttpMeta: { Response: response, Request: req$ },
  };

  const [result$] = await M.match<
    DeleteCollectSubmissionResponseResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >(
    M.json(200, DeleteCollectSubmissionResponseResponse$zodSchema, {
      key: "DeleteCollectSubmissionResponse",
    }),
    M.json(400, DeleteCollectSubmissionResponseResponse$zodSchema, {
      key: "fourHundredApplicationJsonObject",
    }),
    M.json(404, DeleteCollectSubmissionResponseResponse$zodSchema, {
      key: "fourHundredAndFourApplicationJsonObject",
    }),
    M.json(410, DeleteCollectSubmissionResponseResponse$zodSchema, {
      key: "bad_request",
    }),
    M.json(429, DeleteCollectSubmissionResponseResponse$zodSchema, {
      key: "rate_limit",
    }),
    M.nil(500, DeleteCollectSubmissionResponseResponse$zodSchema),
  )(response, req$, { extraFields: responseFields$ });

  return [result$, { status: "complete", request: req$, response }];
}
