/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { SteuerboardCore } from "../core.js";
import { encodeSimple } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { pathToFunc } from "../lib/url.js";
import { APIError } from "../models/errors/apierror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import {
  GetcollectSessionByTokenRequest,
  GetcollectSessionByTokenRequest$zodSchema,
  GetcollectSessionByTokenResponse,
  GetcollectSessionByTokenResponse$zodSchema,
} from "../models/getcollectsessionbytokenop.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * Get collect session by token
 *
 * @remarks
 * Validates the token and returns the Collect-UI session payload if valid.
 */
export function collectSessionsGetcollectSessionByToken(
  client$: SteuerboardCore,
  request: GetcollectSessionByTokenRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    GetcollectSessionByTokenResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >
> {
  return new APIPromise($do(
    client$,
    request,
    options,
  ));
}

async function $do(
  client$: SteuerboardCore,
  request: GetcollectSessionByTokenRequest,
  options?: RequestOptions,
): Promise<
  [
    Result<
      GetcollectSessionByTokenResponse,
      | APIError
      | SDKValidationError
      | UnexpectedClientError
      | InvalidRequestError
      | RequestAbortedError
      | RequestTimeoutError
      | ConnectionError
    >,
    APICall,
  ]
> {
  const parsed$ = safeParse(
    request,
    (value$) => GetcollectSessionByTokenRequest$zodSchema.parse(value$),
    "Input validation failed",
  );
  if (!parsed$.ok) {
    return [parsed$, { status: "invalid" }];
  }
  const payload$ = parsed$.value;
  const body$ = null;

  const pathParams$ = {
    token: encodeSimple("token", payload$.token, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path$ = pathToFunc("/collect/sessions/{token}")(
    pathParams$,
  );

  const headers$ = new Headers(compactMap({
    Accept: "application/json",
  }));

  const context = {
    options: client$._options,
    baseURL: options?.serverURL ?? client$._baseURL ?? "",
    operationID: "getcollectSessionByToken",
    oAuth2Scopes: [],
    resolvedSecurity: null,
    securitySource: null,
    retryConfig: options?.retries
      || client$._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || [
      "429",
      "500",
      "502",
      "503",
      "504",
    ],
  };

  const requestRes = client$._createRequest(context, {
    method: "GET",
    baseURL: options?.serverURL,
    path: path$,
    headers: headers$,
    body: body$,
    userAgent: client$._options.userAgent,
    timeoutMs: options?.timeoutMs || client$._options.timeoutMs
      || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req$ = requestRes.value;

  const doResult = await client$._do(req$, {
    context,
    errorCodes: [],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req$ }];
  }
  const response = doResult.value;
  const responseFields$ = {
    HttpMeta: { Response: response, Request: req$ },
  };

  const [result$] = await M.match<
    GetcollectSessionByTokenResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >(
    M.json(200, GetcollectSessionByTokenResponse$zodSchema, {
      key: "collectSessionByToken",
    }),
    M.json(404, GetcollectSessionByTokenResponse$zodSchema, {
      key: "fourHundredAndFourApplicationJsonObject",
    }),
    M.json(410, GetcollectSessionByTokenResponse$zodSchema, {
      key: "bad_request",
    }),
    M.json(422, GetcollectSessionByTokenResponse$zodSchema, {
      key: "fourHundredAndTwentyTwoApplicationJsonObject",
    }),
    M.json(429, GetcollectSessionByTokenResponse$zodSchema, {
      key: "rate_limit",
    }),
    M.nil(500, GetcollectSessionByTokenResponse$zodSchema),
  )(response, req$, { extraFields: responseFields$ });

  return [result$, { status: "complete", request: req$, response }];
}
