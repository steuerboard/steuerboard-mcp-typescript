
name = "steuerboard-mcp-server"
main = "src/cloudflare-worker/cloudflare-worker.ts"
compatibility_date = "2025-07-15"

# Enable node compatibility for MCP SDK
compatibility_flags = ["nodejs_compat"]

# Durable Object binding for MCP Agent
[[durable_objects.bindings]]
name = "@STEUERBOARD/MCP_MCP"
class_name = "AtSteuerboardMcpMCP"

# Durable Object migration
[[migrations]]
tag = "v2"
new_classes = ["AtSteuerboardMcpMCP"]

# Static assets configuration
[assets]
directory = "./static"
binding = "ASSETS"
run_worker_first = true

# Development configuration
[dev]
port = 8787